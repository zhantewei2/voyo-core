import { RefeshComponent } from "../refresh/refresh.component";
import { assemblyComponent } from "../AssemblyComponent";
export const registryRefresh = () => {
    assemblyComponent("voyoc-refresh", RefeshComponent);
};
