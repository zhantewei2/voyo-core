import { PaginationComponent } from "../pagination/pagination.component";
import { assemblyComponent } from "../AssemblyComponent";

export const registryPagination = () => {
  assemblyComponent("voyoc-pagination", PaginationComponent);
};
